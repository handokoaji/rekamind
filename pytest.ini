[pytest]
asyncio_mode = auto
markers =
    hardware: requires real audio devices, skipped by default
    postgres: requires the real Postgres server, skipped by default
addopts = -m "not hardware and not postgres"
